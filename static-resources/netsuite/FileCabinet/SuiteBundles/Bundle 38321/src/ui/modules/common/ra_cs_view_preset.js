/** * Copyright © 2018, Oracle and/or its affiliates. All rights reserved. */var longDateFormat = RA.App.Context.getPreference('longdateformat');var dailyDateFormat = RA.Util.Date.getDateFormatForDailyPreset(longDateFormat);var weeklyDateFormat = RA.Util.Date.getDateFormatForWeeklyPreset(longDateFormat);var monthlyDateFormat = RA.Util.Date.getDateFormatForMonthlyPreset(longDateFormat);Sch.preset.Manager.registerPreset('RA.ViewPreset.Daily', {	displayDateFormat: "D M d",	shiftUnit: "DAY",	shiftIncrement: 1,	defaultSpan: 14,	timeResolution: {		unit: "DAY",		increment: 1	},	headerConfig: {		middle: {			unit: "DAY",			renderer: function (start, end, headerConfig, index) {				var date = Ext4.Date.format(start, dailyDateFormat);				return date.toString().toUpperCase();			}		}	}});Sch.preset.Manager.registerPreset('RA.ViewPreset.Weekly', {	displayDateFormat: "D M d",	shiftUnit: "WEEK",	shiftIncrement: 1,	defaultSpan: 12,	timeResolution: {		unit: "DAY",		increment: 1	},	headerConfig: {		middle: {			unit: "WEEK",			renderer: function (start, end, headerConfig, index) {				var date = Ext4.Date.format(start, weeklyDateFormat);				return date.toString().toUpperCase();			}		}	}});Sch.preset.Manager.registerPreset('RA.ViewPreset.Monthly', {	displayDateFormat: "D M d",	shiftIncrement: 1,	shiftUnit: "MONTH",	defaultSpan: 12,	timeResolution: {		unit: "DAY",		increment: 1	},	headerConfig: {		middle: {			unit: "MONTH",			renderer: function (start, end, headerConfig, index) {				var date = Ext4.Date.format(start, monthlyDateFormat);				return date.toString().toUpperCase();			}		}	}});